<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Users</title>
    <%- include('../partials/links') %>
    <style>
        body {
            font-family: 'Cormorant Garamond', serif;
            background: linear-gradient(rgba(26, 26, 26, 0.9), rgba(26, 26, 26, 0.9)),
                url('https://images.unsplash.com/photo-1526234362653-3b75a0c07438?q=80&w=1932&auto=format&fit=crop') no-repeat center center fixed;
            background-size: cover;
            color: var(--text-color);
        }

        .table-section {
            background: rgba(26, 26, 26, 0.85);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(197, 168, 128, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .table-section:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
        }

        .luxury-button {
            background: var(--primary-color);
            color: var(--secondary-color);
            padding: 0.8rem 2rem;
            border-radius: 5px;
            font-family: 'Montserrat', sans-serif;
            font-weight: 500;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
            border: 2px solid var(--primary-color);
        }

        .luxury-button:hover {
            background: transparent;
            color: var(--primary-color);
        }

        .luxury-container {
            background: rgba(26, 26, 26, 0.85);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
            border: 1px solid rgba(197, 168, 128, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .luxury-container:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
        }

        .heading-primary {
            font-family: 'Cormorant Garamond', serif;
            font-size: 3.5rem;
            color: var(--primary-color);
            text-align: center;
            margin-bottom: 3rem;
        }

        .form-control {
            padding: 0.75rem 1rem;
            background: rgba(255,255,255,0.1);
            border: 1px solid rgba(197,168,128,0.3);
            border-radius: 5px;
            color: white;
            font-family: 'Montserrat', sans-serif;
            width: 300px;
        }

        .flex {
            display: flex;
        }

        .justify-between {
            justify-content: space-between;
        }

        .items-center {
            align-items: center;
        }

        .mb-8 {
            margin-bottom: 2rem;
        }

        .text-primary-color {
            color: var(--primary-color);
        }

        .py-4 {
            padding-top: 1rem;
            padding-bottom: 1rem;
        }

        .px-6 {
            padding-left: 1.5rem;
            padding-right: 1.5rem;
        }

        .border-b {
            border-bottom: 1px solid rgba(197,168,128,0.3);
        }

        .border-gray-700 {
            border-color: rgba(197,168,128,0.3);
        }

        .hover\:bg-gray-800 {
            transition: background-color 0.3s ease;
        }

        .hover\:bg-gray-800:hover {
            background-color: rgba(26, 26, 26, 0.85);
        }

        .transition-colors {
            transition: color 0.3s ease;
        }
    </style>
</head>

<body class="bg-gray-100">
    <%- include('../partials/navbar') %>

    <div class="admin-container" style="min-height: 100vh; background: linear-gradient(rgba(0,0,0,0.9), rgba(0,0,0,0.9)), url('/assets/img/admin-bg.jpg') center/cover; padding: 6rem 0;">
        <div class="container" style="max-width: 1200px; margin: 0 auto; padding: 0 2rem;">
            <h1 class="heading-primary mb-8 text-center">User Management</h1>

            <div class="admin-content luxury-container p-8">
                <!-- Search and Filter Section -->
                <div class="search-filter flex justify-between items-center mb-8">
                    <input type="text" id="searchInput" placeholder="Search users..." class="form-control">
                </div>

                <div class="users-table overflow-x-auto">
                    <table class="w-full text-left">
                        <thead>
                            <tr class="border-b border-gray-700">
                                <th class="py-4 px-6 text-primary-color">User ID</th>
                                <th class="py-4 px-6 text-primary-color">Username</th>
                                <th class="py-4 px-6 text-primary-color">Email</th>
                                <th class="py-4 px-6 text-primary-color">Role</th>
                                <th class="py-4 px-6 text-primary-color">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% users.forEach(user => { %>
                                <tr class="hover:bg-gray-800 transition-colors">
                                    <td class="py-4 px-6"><%= user._id %></td>
                                    <td class="py-4 px-6"><%= user.username %></td>
                                    <td class="py-4 px-6"><%= user.email %></td>
                                    <td class="py-4 px-6"><%= user.role %></td>
                                    <td class="py-4 px-6">
                                        <a href="/admin/users/<%= user._id %>/edit" class="luxury-button">Edit</a>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>
</body>

</html>
